# Sales stories are separate from the private demo/ SQL and tooling directory.
# Use the canonical extensionless target. An .html target triggers Cloudflare's
# HTML-normalization redirect and loses the original category pathname.
/demo /demo-library 200
/demo/* /demo-library 200
